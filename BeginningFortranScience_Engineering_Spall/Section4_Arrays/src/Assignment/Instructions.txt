Compute the temperature distribution in an L-shaped domain.  Essentially a square region with the upper right quadrant "removed."  Use an iterative technique similar to what we have been using as a demonstration problem for computing the temperature over a square region.  Set all wall temperatures to zero except along the two walls that border the interior of the upper right quadrant.  Use 9 mesh points (unknowns) in each direction which includes the boundary condition values.  One strategy would be to implement the CYCLE statement to redirect the calculation to the top of the DO loops when trying to enter the blocked out upper right quadrant.  Print your results to the screen.